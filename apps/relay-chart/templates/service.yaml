apiVersion: v1
kind: Service
metadata:
  name: {{ include "conduit-relay.fullname" . }}
  labels:
    {{- include "conduit-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
  selector:
    {{- include "conduit-relay.selectorLabels" . | nindent 4 }}
